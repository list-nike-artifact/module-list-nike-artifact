#include <sig.h>
#include <string.h>
#include <encoded_sizes.h>
#include <nike.h>

// wrappers for the NIKE functions to use byte-encoded in- and outputs

int
NIKE_keypair(unsigned char *pk, unsigned char *sk)
{
    int ret = 0;
    return ret;
}

int
NIKE_shared(unsigned long long *pk, const unsigned char *sk)
{
    int ret = 0;
    return ret;
}
